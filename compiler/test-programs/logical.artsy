# Declare variables
declare n as flag;
declare a as int;
declare b as int;
declare x as int;
declare z as int;

# Assign values
let x = 0;
let a = 5;
let b = 3;
let n = (a > b); 

# While and If-Elif-Else test
while (x < 4 and a != 0 and b == 3) {
	if (x < 1) {
		let z = 5;
		print z;
		let x = x + 1;
	}

	elif (x == 1 or x < 0 and a != 0) {
		let z = 6;
		print z;
		let x = x + 1;
	}

	elif (x == 2 and x > 1){
		let z = 7;
		print z;
		let x = x + 1;
	}
	else{
		let z = 8;
		print z;
		let x = x + 1;
		if (n == TRUE) {
			print 1; 
			if ((a < b) == FALSE) {
				print 0; 
			}
		}
	}
}